---
import Layout from '../layouts/Layout.astro';
import { BookOpen, Code, Terminal, Copy, ExternalLink, Server, Globe, Shield } from 'lucide-react';

const baseUrl = Astro.url.origin;
---

<Layout title="API 接口文档 - FlatNas图库">
  <div class="min-h-screen bg-gray-50 p-6 md:p-12">
    <div class="max-w-5xl mx-auto">
      <header className="mb-12 border-b border-gray-200 pb-8">
        <a href="/" className="inline-flex items-center text-blue-600 hover:text-blue-800 mb-4 transition-colors font-medium">
          &larr; 返回首页
        </a>
        <h1 className="text-4xl font-bold text-gray-900 mb-4 flex items-center gap-3">
          <Server className="w-10 h-10 text-blue-600" />
          API 接口文档
        </h1>
        <p className="text-xl text-gray-600">
          后端服务地址 (Base URL): <code class="bg-gray-200 px-2 py-1 rounded text-gray-800 font-mono text-lg">{baseUrl}</code>
        </p>
      </header>

      <div class="space-y-12">
        
        {/* 1. 资源查询接口 */}
        <section class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
          <div class="bg-blue-50 p-6 border-b border-blue-100 flex items-center justify-between">
            <h2 class="text-xl font-bold text-blue-900 flex items-center gap-2">
              <Globe className="w-5 h-5" />
              1. 获取资源列表
            </h2>
            <span class="bg-blue-200 text-blue-800 px-3 py-1 rounded-full text-sm font-bold font-mono">GET /api/icons</span>
          </div>
          
          <div class="p-8">
            <p class="text-gray-600 mb-6">获取图标、壁纸等资源列表，支持分页和搜索。</p>

            {/* 常用场景表格 */}
            <div class="mb-8">
              <h3 class="text-lg font-bold text-gray-800 mb-4 border-l-4 border-blue-500 pl-3">📌 常用场景速查 (对应顶部导航)</h3>
              <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                  <thead>
                    <tr class="bg-gray-100 text-gray-700">
                      <th class="p-3 border-b border-gray-200 w-1/4">UI 按钮</th>
                      <th class="p-3 border-b border-gray-200">完整接口 URL (点击复制)</th>
                    </tr>
                  </thead>
                  <tbody class="text-sm font-mono">
                    <tr class="border-b border-gray-100 hover:bg-gray-50 bg-blue-50">
                      <td class="p-3 text-gray-900 font-sans font-medium font-bold flex items-center gap-2">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-layout-grid"><rect width="7" height="7" x="3" y="3" rx="1"></rect><rect width="7" height="7" x="14" y="3" rx="1"></rect><rect width="7" height="7" x="14" y="14" rx="1"></rect><rect width="7" height="7" x="3" y="14" rx="1"></rect></svg>
                        图标 (全部)
                      </td>
                      <td class="p-3 text-blue-600 select-all font-bold">{baseUrl}/api/icons</td>
                    </tr>
                    <tr class="border-b border-gray-100 hover:bg-gray-50">
                      <td class="p-3 text-gray-900 font-sans font-medium flex items-center gap-2">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-monitor"><rect width="20" height="14" x="2" y="3" rx="2"></rect><line x1="8" x2="16" y1="21" y2="21"></line><line x1="12" x2="12" y1="17" y2="21"></line></svg>
                        PC壁纸
                      </td>
                      <td class="p-3 text-blue-600 select-all">{baseUrl}/api/icons/pc</td>
                    </tr>
                    <tr class="border-b border-gray-100 hover:bg-gray-50">
                      <td class="p-3 text-gray-900 font-sans font-medium flex items-center gap-2">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-smartphone"><rect width="14" height="20" x="5" y="2" rx="2" ry="2"></rect><path d="M12 18h.01"></path></svg>
                        手机壁纸
                      </td>
                      <td class="p-3 text-blue-600 select-all">{baseUrl}/api/icons/mobile</td>
                    </tr>
                    <tr class="border-b border-gray-100 hover:bg-gray-50">
                      <td class="p-3 text-gray-900 font-sans font-medium flex items-center gap-2">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-id-card"><path d="M16 10h2"></path><path d="M16 14h2"></path><path d="M6.17 15a3 3 0 0 1 5.66 0"></path><circle cx="9" cy="11" r="2"></circle><rect x="2" y="5" width="20" height="14" rx="2"></rect></svg>
                        卡片图片
                      </td>
                      <td class="p-3 text-blue-600 select-all">{baseUrl}/api/icons/card</td>
                    </tr>
                  </tbody>
                </table>
              </div>
              <p class="mt-3 text-xs text-gray-500">
                也可以统一使用
                <code class="bg-gray-100 px-1 rounded font-mono text-[11px]">GET /api/icons?category=pc|mobile|card</code>
                按分类获取，便于在同一接口上做分页和搜索。
              </p>
            </div>

            {/* 参数说明 */}
            <div class="mb-8">
              <h3 class="text-lg font-bold text-gray-800 mb-4 border-l-4 border-blue-500 pl-3">⚙️ 请求参数</h3>
              <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <div class="p-4 bg-gray-50 rounded-lg border border-gray-100">
                  <div class="font-mono text-blue-700 font-bold mb-1">search</div>
                  <div class="text-xs text-gray-500 mb-2">String (Optional)</div>
                  <div class="text-sm text-gray-600">搜索关键词。用于在当前分类内按文件名或标签进一步筛选。</div>
                </div>
                <div class="p-4 bg-gray-50 rounded-lg border border-gray-100">
                  <div class="font-mono text-blue-700 font-bold mb-1">page</div>
                  <div class="text-xs text-gray-500 mb-2">Number (Default: 1)</div>
                  <div class="text-sm text-gray-600">当前页码。</div>
                </div>
                <div class="p-4 bg-gray-50 rounded-lg border border-gray-100">
                  <div class="font-mono text-blue-700 font-bold mb-1">limit</div>
                  <div class="text-xs text-gray-500 mb-2">Number (Default: 50)</div>
                  <div class="text-sm text-gray-600">每页返回数量。</div>
                </div>
                <div class="p-4 bg-gray-50 rounded-lg border border-gray-100">
                  <div class="font-mono text-blue-700 font-bold mb-1">category</div>
                  <div class="text-xs text-gray-500 mb-2">String (Optional)</div>
                  <div class="text-sm text-gray-600">资源分类：all/icon、pc、mobile、card。用于按目录筛选。</div>
                </div>
              </div>
            </div>

            {/* 响应示例 */}
            <div>
              <h3 class="text-lg font-bold text-gray-800 mb-4 border-l-4 border-blue-500 pl-3">📦 响应数据 (JSON)</h3>
              <div class="bg-gray-900 rounded-xl p-5 overflow-x-auto text-sm shadow-inner">
<pre class="text-gray-300 font-mono"><code class="language-json">{`{
  "data": [
    {
      "id": "wallpaper/scenery.jpg",          // 唯一标识 (相对路径)
      "name": "scenery.jpg",                  // 文件名
      "url": "/api/image/wallpaper/scenery.jpg", // 图片访问地址 (直接可用)
      "tags": ["PC", "nature"],               // 标签列表
      "mtime": 1715000000000                  // 修改时间戳
    }
  ],
  "pagination": {
    "total": 120,    // 总数量
    "page": 1,       // 当前页
    "limit": 50,     // 每页数量
    "totalPages": 3  // 总页数
  }
}`}</code></pre>
              </div>
            </div>
          </div>
        </section>

        <section class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
           <div class="bg-green-50 p-6 border-b border-green-100 flex items-center justify-between">
            <h2 class="text-xl font-bold text-green-900 flex items-center gap-2">
              <ExternalLink className="w-5 h-5" />
              2. 直接访问图片
            </h2>
            <span class="bg-green-200 text-green-800 px-3 py-1 rounded-full text-sm font-bold font-mono">GET /api/image/[path]</span>
          </div>
          <div class="p-8">
            <p class="text-gray-600 mb-4">
              直接通过 URL 加载图片，支持浏览器直接访问或 <code class="bg-gray-100 px-1 rounded">&lt;img&gt;</code> 标签。
              <br/>
              <strong>注意：</strong> 路径必须匹配返回数据中的 <code class="text-red-500">id</code> 字段。
            </p>
            <div class="bg-gray-100 p-4 rounded-lg font-mono text-gray-700 border border-gray-200 break-all">
              {baseUrl}/api/image/<span class="text-blue-600">path/to/icon.png</span>
            </div>
          </div>
        </section>

        <section class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
          <div class="bg-yellow-50 p-6 border-b border-yellow-100 flex items-center justify-between">
            <h2 class="text-xl font-bold text-yellow-900 flex items-center gap-2">
              <Shield className="w-5 h-5" />
              3. 认证与用户管理
            </h2>
            <span class="text-yellow-800 text-sm font-mono bg-yellow-200 px-3 py-1 rounded-full">/api/auth/* /api/users</span>
          </div>
          <div class="p-8 space-y-6">
            <div>
              <div class="flex items-center gap-3 mb-1">
                <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded text-xs font-bold">POST</span>
                <code class="text-sm font-mono text-gray-800">/api/auth/register</code>
              </div>
              <p class="text-sm text-gray-600 mb-1">注册新用户，默认角色为普通用户，需管理员授权后才能上传。</p>
              <pre class="bg-gray-900 text-gray-200 rounded-lg p-3 text-xs overflow-x-auto font-mono"><code>{`Body (JSON):
{
  "username": "your_name",
  "password": "your_password"
}

Response 200:
{
  "success": true,
  "pendingApproval": true
}`}</code></pre>
            </div>

            <div>
              <div class="flex items-center gap-3 mb-1">
                <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded text-xs font-bold">POST</span>
                <code class="text-sm font-mono text-gray-800">/api/auth/login</code>
              </div>
              <p class="text-sm text-gray-600 mb-1">用户名 + 密码登录，成功后会在 Cookie 中写入会话信息。</p>
              <pre class="bg-gray-900 text-gray-200 rounded-lg p-3 text-xs overflow-x-auto font-mono"><code>{`Body (JSON):
{
  "username": "your_name",
  "password": "your_password"
}

Response 200:
{
  "success": true,
  "user": {
    "username": "your_name",
    "role": "admin | user",
    "approved": true
  }
}`}</code></pre>
            </div>

            <div>
              <div class="flex items-center gap-3 mb-1">
                <span class="bg-purple-100 text-purple-800 px-2 py-1 rounded text-xs font-bold">POST</span>
                <code class="text-sm font-mono text-gray-800">/api/auth/change-password</code>
              </div>
              <p class="text-sm text-gray-600 mb-1">登录后修改当前用户密码。</p>
              <pre class="bg-gray-900 text-gray-200 rounded-lg p-3 text-xs overflow-x-auto font-mono"><code>{`Body (JSON):
{
  "oldPassword": "old",
  "newPassword": "new"
}

Response 200:
{ "success": true }`}</code></pre>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <div class="flex items-center gap-3 mb-1">
                  <span class="bg-gray-100 text-gray-800 px-2 py-1 rounded text-xs font-bold">GET</span>
                  <code class="text-sm font-mono text-gray-800">/api/auth/check</code>
                </div>
                <p class="text-sm text-gray-600 mb-1">检查当前登录状态，返回用户角色和是否已授权上传。</p>
                <pre class="bg-gray-900 text-gray-200 rounded-lg p-3 text-xs overflow-x-auto font-mono"><code>{`Response 200:
{
  "loggedIn": true,
  "user": {
    "username": "xx",
    "role": "admin | user",
    "approved": true
  }
}`}</code></pre>
              </div>

              <div>
                <div class="flex items-center gap-3 mb-1">
                  <span class="bg-gray-100 text-gray-800 px-2 py-1 rounded text-xs font-bold">POST</span>
                  <code class="text-sm font-mono text-gray-800">/api/auth/logout</code>
                </div>
                <p class="text-sm text-gray-600 mb-1">退出登录，清理会话 Cookie。</p>
                <pre class="bg-gray-900 text-gray-200 rounded-lg p-3 text-xs overflow-x-auto font-mono"><code>{`Response 200:
{ "success": true }`}</code></pre>
              </div>
            </div>

            <div>
              <div class="flex items-center gap-3 mb-1">
                <span class="bg-red-100 text-red-800 px-2 py-1 rounded text-xs font-bold">GET / POST</span>
                <code class="text-sm font-mono text-gray-800">/api/users</code>
                <span class="text-xs text-red-500">仅管理员</span>
              </div>
              <p class="text-sm text-gray-600 mb-1">管理员查看用户列表、授权上传权限、调整角色。</p>
              <pre class="bg-gray-900 text-gray-200 rounded-lg p-3 text-xs overflow-x-auto font-mono"><code>{`GET /api/users
Response 200:
{
  "users": [
    {
      "username": "alice",
      "role": "admin",
      "approved": true,
      "createdAt": 1715000000000
    }
  ]
}

POST /api/users
Body (JSON):
{
  "username": "bob",
  "approved": true,          // 可选，授权/关闭上传
  "role": "admin" | "user"   // 可选，调整角色
}

Response 200:
{ "success": true }`}</code></pre>
            </div>

            <p class="text-xs text-gray-500">
              提示：当前端上传接口返回 403 时，通常是用户未登录或未被管理员授权上传。
            </p>
          </div>
        </section>

        <section class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden opacity-75 hover:opacity-100 transition-opacity">
           <div class="bg-gray-50 p-6 border-b border-gray-200 flex items-center justify-between">
            <h2 class="text-xl font-bold text-gray-700 flex items-center gap-2">
              <Shield className="w-5 h-5" />
              4. 管理接口 (需鉴权)
            </h2>
            <span class="text-gray-500 text-sm">仅管理员</span>
          </div>
          <div class="p-8 space-y-6">
            <div>
              <div class="flex items-center gap-3 mb-2">
                <span class="bg-yellow-100 text-yellow-800 px-2 py-1 rounded text-xs font-bold">POST</span>
                <code class="text-sm font-mono text-gray-800">/api/icons</code>
                <span class="text-sm text-gray-500">- 上传文件 (Multipart/Form-Data)</span>
              </div>
            </div>
             <div>
              <div class="flex items-center gap-3 mb-2">
                <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded text-xs font-bold">PUT</span>
                <code class="text-sm font-mono text-gray-800">/api/icons/[id]</code>
                <span class="text-sm text-gray-500">- 更新标签 (JSON Body: {`{ "tags": [...] }`})</span>
              </div>
            </div>
             <div>
              <div class="flex items-center gap-3 mb-2">
                <span class="bg-red-100 text-red-800 px-2 py-1 rounded text-xs font-bold">DELETE</span>
                <code class="text-sm font-mono text-gray-800">/api/icons/[id]</code>
                <span class="text-sm text-gray-500">- 删除文件</span>
              </div>
            </div>
          </div>
        </section>

        <section class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
          <div class="bg-purple-50 p-6 border-b border-purple-100 flex items-center justify-between">
            <h2 class="text-xl font-bold text-purple-900 flex items-center gap-2">
              <Code className="w-5 h-5" />
              5. 快速接入示例 (JavaScript/React)
            </h2>
          </div>
          <div class="p-8">
             <p class="text-gray-600 mb-6">在您的前端项目中，可以直接使用 <code class="bg-gray-100 px-1 rounded">fetch</code> 调用接口。API 支持跨域 (CORS)，可在任意域名下使用。</p>
             
             <div class="bg-gray-900 rounded-xl p-5 overflow-x-auto text-sm shadow-inner relative group">
                <div class="absolute top-4 right-4 text-gray-400 text-xs font-sans">JavaScript</div>
<pre class="text-gray-300 font-mono"><code class="language-javascript">{`const API_BASE = "${baseUrl}";

async function fetchIcons() {
  const response = await fetch(\`\${API_BASE}/api/icons\`);
  const result = await response.json();
  return result.data.map(item => ({
    ...item,
    fullUrl: \`\${API_BASE}\${item.url}\`
}));
}

async function fetchPcWallpapers() {
  const response = await fetch(\`\${API_BASE}/api/icons?category=pc\`);
  const result = await response.json();
  return result.data.map(item => ({
    ...item,
    fullUrl: \`\${API_BASE}\${item.url}\`
}));
}

async function fetchMobileWallpapers() {
  const response = await fetch(\`\${API_BASE}/api/icons?category=mobile\`);
  const result = await response.json();
  return result.data.map(item => ({
    ...item,
    fullUrl: \`\${API_BASE}\${item.url}\`
}));
}

async function fetchCardImages() {
  const response = await fetch(\`\${API_BASE}/api/icons?category=card\`);
  const result = await response.json();
  return result.data.map(item => ({
    ...item,
    fullUrl: \`\${API_BASE}\${item.url}\`
  }));
}`}</code></pre>
             </div>
          </div>
        </section>

      </div>
    </div>
  </div>
</Layout>
